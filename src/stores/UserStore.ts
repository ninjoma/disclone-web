import {createStore} from 'vuex'

export default createStore({
    state: {
        user: {
            id: "test",
            username: String,
            email: String,
            token: String
        }
    },
    getters: {},
    mutations: {},
    actions: {}
})